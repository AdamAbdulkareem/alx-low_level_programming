#include <stdio.h>
/**
 * mul - A function that perform multiplication
 * @a: An input character
 * @b: An input character
 * Return: Integer
**/
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}

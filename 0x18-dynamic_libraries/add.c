#include <stdio.h>
/**
  * add - A function that perform addition
  * @a: An input character
  * @b: An input character
  * Return: Integer
**/
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

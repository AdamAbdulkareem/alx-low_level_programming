#include <stdio.h>
/**
 * mod - A function that perform modulus
 * @a: An input character
 * @b: An input character
 * Return: Integer
*/
int mod(int a, int b)
{
	int result;

	result = a % b;
	return (result);
}

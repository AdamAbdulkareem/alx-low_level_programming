#include <stdio.h>
/**
 * sub - A function that perform subtraction
 * @a: An input character
 * @b: An input character
 * Return: Integer
**/
int sub(int a, int b)
{
	int result;

	result = a - b;
	return (result);
}

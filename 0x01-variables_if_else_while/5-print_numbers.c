#include <stdio.h>
/**
  * main - Entry point of the program
  * Return: Always 0
  */
int main(void)
{
	int digit;
		for (digit = 0; digit <= 9; digit++)
		{
			printf("%d", digit);
		}
	return (0);
}

#include <stdio.h>
/**
  * main - Entry point of the program
  * Return: Always 0
  */
int main(void)
{
	char lowerChar;
		for (lowerChar = 'a'; lowerChar <= 'z'; letter++)
		{
		if (letter != 'e' && letter != 'q')
			putchar(letter);
		}

	putchar('\n');
	return (0);
}
